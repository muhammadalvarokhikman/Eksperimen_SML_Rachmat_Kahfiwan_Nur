name: eksperimen_sml_env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - xgboost
      - mlflow
      - dagshub
